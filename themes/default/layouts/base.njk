<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  {# SEO Meta Tags #}
  <title>{{ seo.title }} | {{ site.site_name }}</title>
  <meta name="description" content="{{ seo.description }}">
  <meta name="robots" content="{{ seo.robots }}">
  <link rel="canonical" href="{{ seo.canonical }}">

  {# Open Graph #}
  <meta property="og:type" content="{{ seo.og.type }}">
  <meta property="og:title" content="{{ seo.og.title }}">
  <meta property="og:description" content="{{ seo.og.description }}">
  <meta property="og:url" content="{{ seo.og.url }}">
  {% if seo.og.image %}
  <meta property="og:image" content="{{ seo.og.image }}">
  {% endif %}
  <meta property="og:site_name" content="{{ site.site_name }}">

  {# Google Analytics #}
  {% if site.google_analytics_id %}
  <script async src="https://www.googletagmanager.com/gtag/js?id={{ site.google_analytics_id }}"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', '{{ site.google_analytics_id }}');
  </script>
  {% endif %}

  {# Fonts #}
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  {% if template.options.google_font_body or template.options.google_font_heading %}
    <link href="https://fonts.googleapis.com/css2?family={{ (template.options.google_font_body or 'Inter') | replace(' ', '+') }}:wght@400;500;600;700&{% if template.options.google_font_heading and template.options.google_font_heading != template.options.google_font_body %}family={{ template.options.google_font_heading | replace(' ', '+') }}:wght@400;500;600;700&{% endif %}display=swap" rel="stylesheet">
  {% endif %}

  {# Theme CSS #}
  {% for cssFile in theme_css %}
  <link rel="stylesheet" href="{{ cssFile }}">
  {% endfor %}

  <style>
    :root {
      {# 
         DIRECT MAPPING - No logic here, just output what the server resolved.
         Note: we prioritize google_font_* if provided, else use body_font.
      #}
      --cms-font-body: {% if template.options.google_font_body %}"{{ template.options.google_font_body }}", sans-serif{% else %}{{ template.options.body_font | default("system-ui, sans-serif") | safe }}{% endif %};
      --cms-font-heading: {% if template.options.google_font_heading %}"{{ template.options.google_font_heading }}", sans-serif{% elif template.options.google_font_body %}"{{ template.options.google_font_body }}", sans-serif{% else %}var(--cms-font-body){% endif %};
      
      --cms-primary-color: {{ template.options.primary_color }};
      --cms-secondary-color: {{ template.options.secondary_color }};
      --cms-color-text: {{ template.options.body_color }};
      --cms-base-font-size: {{ template.options.body_size }};
      --cms-max-width: {{ template.options.container_width }};
      
      --h1-size: {{ template.options.h1_size }};
      --h1-color: {{ template.options.h1_color | default(template.options.body_color) }};
      --h1-weight: {{ template.options.h1_weight }};
      
      --h2-size: {{ template.options.h2_size | default('2rem') }};
      --h3-size: {{ template.options.h3_size | default('1.5rem') }};
      
      --p-size: {{ template.options.p_size | default('1rem') }};
      --link-color: {{ template.options.link_color }};
    }

    body {
      font-family: var(--cms-font-body);
      font-size: var(--cms-base-font-size);
      color: var(--cms-color-text);
      line-height: 1.6;
    }

    h1, h2, h3 { font-family: var(--cms-font-heading); }
    h1 { font-size: var(--h1-size); color: var(--h1-color); font-weight: var(--h1-weight); }
    p { font-size: var(--p-size); }
    a { color: var(--link-color); }

    {% block styles %}{% endblock %}
  </style>
</head>
<body{% if user and page %} data-page-id="{{ page.id }}" data-template-id="{{ template.id }}" data-content-type="{{ content_type | default('pages') }}"{% endif %}>
  <header class="site-header">
    <div class="container">
      <a href="/" class="site-logo">{{ site.site_name }}</a>
      <nav class="site-nav">
        <ul>
          {% if menus['main-nav'] and menus['main-nav'].items %}
            {% for item in menus['main-nav'].items %}
              <li><a href="{{ item.url }}" {% if item.target %}target="{{ item.target }}"{% endif %}>{{ item.title }}</a></li>
            {% endfor %}
          {% else %}
            <li><a href="/">Home</a></li>
            <li><a href="/pages/about">About</a></li>
            <li><a href="/pages/contact">Contact</a></li>
          {% endif %}
        </ul>
      </nav>
    </div>
  </header>

  <main class="site-main">
    {% block content %}{% endblock %}
  </main>

  <footer class="site-footer">
    <div class="container">
      <p>&copy; <script>document.write(new Date().getFullYear());</script> {{ site.site_name }}. All rights reserved.</p>
      
      {# Design Mode Toolbar #}
      {% if user and (user.role == 'admin' or user.role == 'editor') %}
      <div class="admin-design-tools" style="margin-top: 2rem; padding-top: 2rem; border-top: 1px solid #eee; display: flex; gap: 2rem; align-items: center; justify-content: center;">
        <span style="font-size: 0.75rem; font-weight: 700; text-transform: uppercase; color: #999;">Design Mode:</span>
        <div data-cms-option="primary_color" data-cms-option-type="color" data-cms-value="{{ template.options.primary_color }}">
          <div style="width: 20px; height: 20px; background: var(--cms-primary-color); border-radius: 4px;"></div>
        </div>
        <div data-cms-option="secondary_color" data-cms-option-type="color" data-cms-value="{{ template.options.secondary_color }}">
          <div style="width: 20px; height: 20px; background: var(--cms-secondary-color); border-radius: 4px;"></div>
        </div>
      </div>
      {% endif %}
    </div>
  </footer>

  {% block scripts %}{% endblock %}

{% if user %}
  <link rel="stylesheet" href="/css/edit-in-place.css">
  <script src="/js/edit-in-place.js"></script>
{% endif %}
</body>
</html>
