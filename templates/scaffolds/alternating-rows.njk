{% extends "layouts/base.njk" %}

{% block styles %}
.page-header {
  padding: 6rem 0 3rem;
  background: var(--color-background);
}

.page-header h1 {
  font-size: 3.5rem;
  text-align: center;
}

.alternating-section {
  padding: 6rem 0 8rem;
}

.row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  align-items: center;
  gap: 6rem;
  margin-bottom: 8rem;
}

.row:last-child {
  margin-bottom: 0;
}

.row.reverse .row-image {
  order: 2;
}

.row.reverse .row-content {
  order: 1;
}

.row-image img {
  width: 100%;
  border-radius: 1rem;
  box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
}

.row-content h2 {
  font-size: 2.5rem;
  margin-bottom: 1.5rem;
}

.row-content p {
  font-size: 1.125rem;
  line-height: 1.8;
  color: var(--color-text-muted);
  margin-bottom: 1.5rem;
}

@media (max-width: 991px) {
  .row { grid-template-columns: 1fr; gap: 3rem; text-align: center; }
  .row.reverse .row-image { order: -1; }
  .row-image { order: -1; }
}
{% endblock %}

{% block content %}
<header class="page-header">
  <div class="container">
    <h1 data-cms-region="page_title" data-cms-type="text" data-cms-label="Page Title">
      {{ content.page_title | default(page.title) }}
    </h1>
  </div>
</header>

<div class="container">
  <section class="alternating-section">
    <div data-cms-region="alternating_rows" 
         data-cms-type="repeater" 
         data-cms-label="Alternating Content Rows"
         data-cms-fields='[{"name":"title","type":"text","label":"Row Title"},{"name":"text","type":"richtext","label":"Row Text"},{"name":"image","type":"image","label":"Row Image"}]'>
      
      {% for item in content.alternating_rows %}
        <div class="row {% if loop.index0 % 2 != 0 %}reverse{% endif %}">
          <div class="row-image">
            <img src="{{ item.image | default('/images/placeholders/600x400.svg') }}" alt="{{ item.title }}">
          </div>
          <div class="row-content">
            <h2>{{ item.title }}</h2>
            <article>
              {{ item.text | safe }}
            </article>
          </div>
        </div>
      {% else %}
        <div class="row">
          <div class="row-image">
            <img src="/images/placeholders/600x400.svg" alt="Feature One">
          </div>
          <div class="row-content">
            <h2>Feature One</h2>
            <p>Showcase your first primary feature or benefit here with a compelling description and accompanying image. This layout keeps the user engaged by changing the focal point in each section.</p>
          </div>
        </div>
        
        <div class="row reverse">
          <div class="row-image">
            <img src="/images/placeholders/600x400.svg" alt="Feature Two">
          </div>
          <div class="row-content">
            <h2>Feature Two</h2>
            <p>The second feature switches sides, creating a dynamic visual rhythm. Use this space to highlight another aspect of your product or service that adds value for your customers.</p>
          </div>
        </div>
      {% endfor %}
    </div>
  </section>
</div>
{% endblock %}

